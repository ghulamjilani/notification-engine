source "https://rubygems.org"
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in notification_engine.gemspec.
gemspec

# Used for Authorization
gem 'cancancan'

# Used for Authentication
gem 'devise'

# Used to start rails server
gem 'puma'

# Used for roles
gem 'rolify'

# Used for best practices
gem 'rubocop'

# Used for database
gem 'sqlite3'

# Start debugger with binding.b [https://github.com/ruby/debug]
# gem "debug", ">= 1.0.0"
